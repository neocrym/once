Once, a library for memoization in Python
=========================================

Once is a library that memoizes repeated functions calls in Python, and is also able to load and save the memoization cache to disk.